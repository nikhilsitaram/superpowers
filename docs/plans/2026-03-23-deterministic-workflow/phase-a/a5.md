# A5: Add review loop protocol to orchestrate SKILL.md

**Avoid:** Don't embed the full review loop pseudocode in SKILL.md — the file is at 992 words and the 1000-word cap is hard. Describe the protocol concisely and reference the design doc for the full loop specification. Use bullet points, not prose paragraphs.

**Avoid:** Don't move the implementation-review dispatch location. The design doc says "orchestrate dispatches implementation-review" — this is already the case (steps 6-9). What changes is: (1) the reviewer now outputs a `json review-summary` block, (2) orchestrate parses it, (3) orchestrate writes reviews.json, and (4) orchestrate calls `--check-review` before advancing phase status.

**Avoid:** Don't duplicate the review protocol description. Write it once as a named protocol section and reference it from both the per-phase, single-phase, and after-all-phases flows.

## Steps

### Step 1: Understand current structure and word budget

Read `skills/orchestrate/SKILL.md`. Current word count: ~992. Budget for new content: ~8 words net (can add more by compressing existing prose).

Key sections to modify:
- Per-Phase Execution steps 6-9 (implementation-review dispatch, triage, re-review gate)
- Single-Phase Plans step 3 (currently just "Implementation review, triage, fix")
- After All Phases steps 2-3 (final cross-phase review)
- Setup section (add reviews.json init)
- Need to add: Review Loop Protocol section, reviews.json writing, --check-review calls, --check-workflow calls

### Step 2: Compress existing prose to make room (~130 words needed)

Apply these specific compressions:

**Permission Model** (current ~35 words → ~15 words):
Current:
```text
Subagents run in `auto` mode. PreToolUse hook (`hooks/pretooluse-safe-commands.sh`) auto-approves Bash commands matching safe list prefixes. Phase dispatcher surfaces non-safe commands after each task for user to grow safe list.
```
Replace with:
```text
Subagents run in `auto` mode with safe-command auto-approval via PreToolUse hook. Phase dispatcher surfaces non-safe commands after each task.
```
Saves ~15 words.

**Progress Tracking** (current ~45 words → ~25 words):
Current:
```text
1. **Read plan** — identify phases and task counts
2. **Build task list** — TaskCreate per phase: "Execute tasks ({N})", "Implementation review", "Create PR". Final: "Mark plan complete". Set `addBlockedBy` so phases block the next.
3. **Update as you go** — mark `in_progress` / `completed`. After subagents, output progress note.
```
Replace with:
```text
TaskCreate per phase: "Execute tasks ({N})", "Implementation review", "Create PR". Final: "Mark plan complete". Set `addBlockedBy`. Mark `in_progress` / `completed` as you go.
```
Saves ~20 words.

**Integration section** (current ~25 words → ~15 words):
Current:
```text
**Workflow:** design (creates feature or integration branch + worktree) → draft-plan → **this skill** → create-pr → review-pr → merge-pr

**See:** `tdd.md` — TDD reference; content is embedded in implementer prompts
```
Replace with:
```text
**Workflow:** design → draft-plan → **this skill** → create-pr → review-pr → merge-pr
**See:** `tdd.md` — TDD reference embedded in implementer prompts
```
Saves ~10 words.

**Wave Loop text block** (current ~55 words → ~40 words):
Current step d:
```text
d. Process completions SERIALLY: review → triage → rebase → create-pr → poll checks → review-pr → merge → mark complete
```
Replace with:
```text
d. Process completions SERIALLY: review loop → rebase → create-pr → poll → review-pr → merge → mark complete
```
Saves ~5 words.

**Per-phase steps 6-9** (current ~60 words → ~30 words — see Step 4 below for replacement).

**After All Phases steps 2-3** (current ~15 words → ~10 words — see Step 5 below).

**Key Constraints table** — remove "Reconciliation before dispatch" row (the reconciliation is already described in the wave loop). Saves ~10 words.

Total savings: ~80-100 words. Combined with the ~30 words saved from steps 6-9 replacement, this creates ~130 words of headroom.

### Step 3: Add Review Loop Protocol section

Add after the Per-Phase Execution section and before Single-Phase Plans. This is the shared protocol referenced by all three flows:

```markdown
## Review Loop Protocol

After each impl-review dispatch:

1. Extract last `json review-summary` fenced block from response. Missing/malformed → verdict:fail, re-dispatch.
2. Triage issues: "fix" (dispatch implementer) or "dismiss" (with reasoning)
3. actionable == 0 → write reviews.json record with verdict:pass, advance
4. actionable 1-5 → fix all, verify, write record verdict:pass, advance
5. actionable > 5 → fix all, write record verdict:fail, re-dispatch (max 3 iterations, then escalate via AskUserQuestion)

Append record to `{PLAN_DIR}/reviews.json`:
`{"type":"impl-review","scope":"{SCOPE}","iteration":N,"issues_found":N,"severity":{...},"actionable":N,"dismissed":N,"dismissals":[...],"fixed":N,"remaining":0,"verdict":"pass|fail","timestamp":"ISO8601"}`
```

### Step 4: Update per-phase execution steps

Replace current steps 6-9:

```markdown
6. Dispatch implementation-review with `PHASE_BASE_SHA`, `HEAD`, `PLAN_DIR`, `PHASE_DIR`, DESIGN_DOC_PATH
7. Run Review Loop Protocol (scope: `phase-{letter_lower}`)
8. Verify: `scripts/validate-plan --check-review plan.json --type impl-review --scope phase-{letter_lower}`
```

Renumber old step 10 → 9, old 11 → 10, etc. through step 18 (becomes 15).

### Step 5: Update Single-Phase Plans section

Current step 3 says "Implementation review, triage, fix" — this is the exact prose-only enforcement the design is replacing. Update to:

```markdown
3. Dispatch implementation-review, run Review Loop Protocol (scope: `phase-a`)
4. `scripts/validate-plan --check-review plan.json --type impl-review --scope phase-a`
5. Run plan criteria: `scripts/validate-plan --criteria plan.json --plan`
6. `scripts/validate-plan --update-status plan.json --plan --status Complete`
7. Route on workflow:
   - `"create-pr"`: invoke create-pr (targets main), `scripts/validate-plan --check-workflow plan.json`, stop
   - `"merge-pr"`: invoke create-pr, poll checks + review-pr (skip if `review_wait_minutes` is 0), then merge-pr with `--squash`, `scripts/validate-plan --check-workflow plan.json`
```

### Step 6: Update After All Phases section

Replace steps 2-3 with:

```markdown
2. Final review: dispatch implementation-review with `PLAN_BASE_SHA..HEAD`, run Review Loop Protocol (scope: `final`)
3. `scripts/validate-plan --check-review plan.json --type impl-review --scope final`
```

Add `--check-workflow` as final action in step 5 routing:

```markdown
5. Route on workflow:
   - `"merge-pr"`: `cd "$MAIN_REPO"` first, create final PR, poll checks, review-pr, merge-pr with `--rebase`, `scripts/validate-plan --check-workflow plan.json`, clean up
   - `"create-pr"`: create final PR, `scripts/validate-plan --check-workflow plan.json`, stop
```

### Step 7: Add reviews.json initialization to Setup

After reading the workflow field:

```markdown
- Initialize: `PLAN_DIR=$(dirname "$(realpath plan.json)")` and `[ -f "$PLAN_DIR/reviews.json" ] || echo '[]' > "$PLAN_DIR/reviews.json"`
```

### Step 8: Verify word count

```bash
wc -w skills/orchestrate/SKILL.md
```

Must be <= 1000. If over, compress further. Remaining candidates:
- Remove the Prompt Templates table heading row and merge into a single line per template
- Shorten Phase DAG Construction section

### Step 9: Commit

```text
feat(orchestrate): add deterministic review loop protocol and workflow gates
```
