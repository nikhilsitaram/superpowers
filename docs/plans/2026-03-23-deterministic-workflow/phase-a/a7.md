# A7: Move plan-review dispatch from draft-plan to design, add --check-workflow for plan-only

**Avoid:** Don't remove the `scripts/validate-plan --schema` and `--render` calls from draft-plan. Those are structural validation and rendering steps that stay in draft-plan. Only the plan-review dispatch (step 9) and the Plan Review Gate section are removed.

**Avoid:** Don't add plan-review dispatch as a subagent-of-subagent. The design doc's "Centralized Review Dispatch" principle says the design skill dispatches plan-review directly: `design → draft-plan (sub) → plan-review (sub)` — both are direct children of design, not nested.

**Avoid:** Don't add the full review loop pseudocode to design SKILL.md (already at 868 words, budget is tight). Reference the protocol concisely.

## Steps

### Step 1: Remove plan-review dispatch from draft-plan SKILL.md

Open `skills/draft-plan/SKILL.md`. Make two changes:

**Change 1:** Remove step 9 from the Workflow section. Current steps 9-10:

```text
9. **Run plan review** — Dispatch reviewer, fix issues until clean
10. **Hand off to execution** — Report plan path to user
```

Replace with:

```text
9. **Hand off** — Report plan path to caller. Plan-review is dispatched by the design skill after draft-plan returns.
```

**Change 2:** Remove the "Plan Review Gate" section entirely (currently the section after "The Fresh Claude Test" starting with "After creating all files, validate structure then dispatch LLM review:"). This section includes:
- Run `scripts/validate-plan --schema`
- Run `scripts/validate-plan --render`
- Dispatch plan-review

The schema and render calls are already in steps 7 and 8. The plan-review dispatch moves to design. Remove from "## Plan Review Gate" through "Skipping validation or review risks plans with missing paths, ordering bugs, or vague done-when conditions reaching execution."

**Change 3:** Remove the "After Review Passes" section (the final section saying "Report the plan file path to the user..."). This is replaced by the new step 9.

Verify word count stays at or under 1000 (should decrease by ~80 words from the removed sections).

### Step 2: Add plan-review dispatch to design SKILL.md

Open `skills/design/SKILL.md`. After step 11 (dispatch draft-plan subagent, currently around line 66-70), add a new step:

```text
11. **Dispatch plan-review subagent** — fresh Opus agent validates the plan
12. **Route workflow** — ...
```

(Renumber existing step 12 to 13, etc.)

The plan-review dispatch follows the same review loop protocol as design-review but runs autonomously (not user-collaborative). Add after the draft-plan dispatch block:

```text
After draft-plan returns, dispatch plan-review:

```
Agent(
  subagent_type: "general-purpose",
  model: "opus",
  prompt: "Review the plan at docs/plans/<folder>/plan.json
    using the plan-review skill.
    Design doc: docs/plans/<folder>/design-<topic>.md
    Working directory: .claude/worktrees/<feature>"
)
```

Run review loop: extract `json review-summary` block, triage issues (fix plan files or dismiss with reasoning). If >5 actionable issues, fix and re-dispatch reviewer (max 3 iterations, then escalate to user). Write review record to `{PLAN_DIR}/reviews.json` with type: "plan-review", scope: "plan".
```

### Step 3: Add design-review record writing

The design skill already dispatches design-review (step 10) and loops until clean. Add reviews.json record writing after design-review passes:

After the existing design-review dispatch block (around line 60), add instruction to:
1. Extract the last `json review-summary` fenced block from the design-review subagent response
2. After the user-collaborative fix loop produces a passing review, write a review record to `{PLAN_DIR}/reviews.json`:
   ```json
   {"type":"design-review","scope":"design","iteration":N,"issues_found":N,"severity":{...},"actionable":N,"dismissed":N,"dismissals":[...],"fixed":N,"remaining":0,"verdict":"pass","timestamp":"ISO8601"}
   ```
3. Initialize reviews.json if it doesn't exist: `[ -f "$PLAN_DIR/reviews.json" ] || echo '[]' > "$PLAN_DIR/reviews.json"`
4. Append with: `jq --argjson entry "$RECORD" '. += [$entry]' "$PLAN_DIR/reviews.json" > tmp && mv tmp "$PLAN_DIR/reviews.json"`

The design-review loop is user-collaborative (the user sees findings and helps fix), so the record is written after the final passing review only.

### Step 4: Add --check-workflow for plan-only

In the workflow routing section (step 12/13), update the plan-only path:

Current:
```text
For **Plan only**: report the plan file path and stop.
```

New:
```text
For **Plan only**: `scripts/validate-plan --check-workflow plan.json` — verifies design-review and plan-review passed. Report the plan file path and stop.
```

### Step 5: Verify word counts

```bash
wc -w skills/design/SKILL.md
wc -w skills/draft-plan/SKILL.md
```

Both must be <= 1000.

### Step 6: Commit

```text
refactor: move plan-review dispatch from draft-plan to design, add workflow gate
```
