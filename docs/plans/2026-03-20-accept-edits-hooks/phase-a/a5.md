## Handoff from A4

Infrastructure in place:
- `hooks/hooks.json` — declares PostToolUse (AskUserQuestion matcher) and PermissionRequest (Edit|Write matcher) hooks
- `hooks/post-tool-use-design-approval.sh` — detects design approval via `metadata.source == "design-approval"` OR `"Plan dir:"` text in question, creates `$plan_dir/.design-approved` sentinel
- `hooks/permission-request-accept-edits.sh` — searches for sentinel, grants acceptEdits mode when session_id matches
- `.claude-plugin/marketplace.json` — claude-caliper and claude-caliper-workflow plugins have `"hooks": "./hooks/hooks.json"`

The design skill's AskUserQuestion approval gate must include both `metadata: { source: "design-approval" }` AND the absolute `Plan dir: /path/to/.claude/worktrees/branch/docs/plans/YYYY-MM-DD-topic` in the question text so the PostToolUse hook can detect and extract the plan dir path.

# A5: Update design skill with structured approval gate

**Avoid:** Don't change the content or order of steps 1-5 (explore, challenge, ask, propose, present) or steps 8-10 (write doc, design-review, draft-plan) — only the approval gate and worktree steps change position.

**Avoid:** Don't add verbose AskUserQuestion JSON examples to SKILL.md — the file has a 1,000-word hard cap per CLAUDE.md conventions. Keep the approval gate instruction concise: show the question format inline with the checklist step, not as a separate block.

## Steps

### Step 1: Modify the checklist in skills/design/SKILL.md

Open `skills/design/SKILL.md`. The current checklist (lines 20-31) reads:

```markdown
1. **Explore context** — files, docs, recent commits
2. **Challenge assumptions** — question the framing before accepting it
3. **Ask clarifying questions** — smart batches (see below)
4. **Propose 2-3 approaches** — trade-offs and your recommendation
5. **Present design** — sections scaled to complexity, approval after each
6. **Get verbal approval** — explicit "yes" before proceeding
7. **Set up worktree** — `git worktree add .claude/worktrees/<branch-name> -b <branch-name>`; run tests to establish a clean baseline
8. **Write design doc** — `docs/plans/YYYY-MM-DD-<topic>/design-<topic>.md`, commit
9. **Dispatch design-review subagent** — fresh Opus agent validates design before planning (hard gate)
10. **Dispatch draft-plan subagent** — fresh Opus agent with design doc path and worktree path (zero design context)
```

Replace steps 6 and 7 with the reordered + restructured versions:

```markdown
6. **Set up worktree** — `git worktree add .claude/worktrees/<branch-name> -b <branch-name>`; run tests to establish a clean baseline
7. **Design approval gate** — use AskUserQuestion with options `["Approved", "Needs changes"]`, include `metadata: { source: "design-approval" }` and absolute plan dir path in question text (format: `Plan dir: /absolute/path/.claude/worktrees/branch/docs/plans/YYYY-MM-DD-topic`). If "Needs changes," return to step 5. The PostToolUse hook creates a `.design-approved` sentinel enabling auto-approved edits for the rest of the session.
```

Steps 8-10 remain unchanged (renumbered from the prior 8-10, so no number changes needed since step 6/7 were just swapped).

### Step 2: Add AskUserQuestion format example

After the existing `Agent(...)` blocks for design-review and draft-plan dispatch (around line 53), add a concise format reference for the approval gate:

```markdown
**Approval gate format:**
```json
{
  "questions": [{
    "question": "Design approved? Plan dir: <absolute-worktree-path>/docs/plans/YYYY-MM-DD-topic",
    "options": [
      { "label": "Approved", "description": "Write design doc and proceed to review" },
      { "label": "Needs changes", "description": "Continue iterating on the design" }
    ]
  }],
  "metadata": { "source": "design-approval" }
}
```

### Step 3: Verify the changes

```bash
grep -q 'metadata' skills/design/SKILL.md
grep -q 'design-approval' skills/design/SKILL.md
grep -q 'Plan dir:' skills/design/SKILL.md
grep -q 'Set up worktree' skills/design/SKILL.md
```

All four greps should succeed. Verify the word count is still under 1,000:

```bash
wc -w skills/design/SKILL.md
```

### Step 4: Run all hook tests to confirm nothing broke

```bash
bash tests/hooks/test_post_tool_use.sh && bash tests/hooks/test_permission_request.sh
```

### Step 5: Commit

```text
feat: update design skill with structured approval gate and worktree-first ordering
```
