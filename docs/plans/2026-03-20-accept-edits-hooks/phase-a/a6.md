## Handoff from A4

`.claude-plugin/marketplace.json` now has `"hooks": "./hooks/hooks.json"` on plugins[0] (claude-caliper) and plugins[1] (claude-caliper-workflow). All three plugins are still at version `1.6.0`. This task bumps all three to `1.7.0`.

# A6: Version bump in marketplace.json

**Avoid:** Don't bump only some plugins — all three plugin entries (`claude-caliper`, `claude-caliper-workflow`, `claude-caliper-tooling`) share the same version and must be bumped together. The plugin installer caches by version, so a stale version means users stay on old behavior.

## Steps

### Step 1: Bump version in marketplace.json

Open `.claude-plugin/marketplace.json`. Change all three `"version": "1.6.0"` entries to `"version": "1.7.0"`.

Verify:

```bash
jq -r '.plugins[].version' .claude-plugin/marketplace.json
```

Expected output — three lines, all `1.7.0`:

```text
1.7.0
1.7.0
1.7.0
```

### Step 2: Run full verification suite

```bash
bash tests/hooks/test_post_tool_use.sh && bash tests/hooks/test_permission_request.sh
```

Also verify existing validate-plan tests still pass:

```bash
bash tests/validate-plan/test_schema.sh && bash tests/validate-plan/test_render.sh && bash tests/validate-plan/test_e2e.sh
```

### Step 3: Commit

```text
chore: bump plugin version to 1.7.0 for acceptEdits hooks
```
