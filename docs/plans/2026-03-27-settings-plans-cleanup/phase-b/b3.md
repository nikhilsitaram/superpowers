# B3: Design-review and plan-review skills: new plan path references

**Avoid:** Don't change the review checklist items, output format, or any review logic. This task only updates path references in example text.

## Steps

### Step 1: Update design-review SKILL.md

In `skills/design-review/SKILL.md`, replace the `docs/plans/` reference.

**Line 23, current:**

```markdown
- **Design doc** — `docs/plans/YYYY-MM-DD-topic/design-topic.md`
```

**New:**

```markdown
- **Design doc** — `.claude/claude-caliper/YYYY-MM-DD-topic/design-topic.md`
```

This is the only `docs/plans/` reference in this file.

### Step 2: Update plan-review SKILL.md

In `skills/plan-review/SKILL.md`, replace the `docs/plans/` reference.

**Line 29, current:**

```markdown
- **Plan directory** — `docs/plans/YYYY-MM-DD-topic/` (containing plan.json + task .md files)
```

**New:**

```markdown
- **Plan directory** — `.claude/claude-caliper/YYYY-MM-DD-topic/` (containing plan.json + task .md files)
```

This is the only `docs/plans/` reference in this file.

### Step 3: Verify changes

```bash
grep -c 'docs/plans/' skills/design-review/SKILL.md skills/plan-review/SKILL.md
```

Both should output `0`.

```bash
grep '.claude/claude-caliper/' skills/design-review/SKILL.md skills/plan-review/SKILL.md
```

Both should show the updated path references.
