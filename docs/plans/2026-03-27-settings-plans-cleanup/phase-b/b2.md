# B2: Draft-plan skill: new plan path and move schema reference

**Avoid:** Don't delete the original schema reference doc at `docs/plans/2026-03-19-structured-plans/design-structured-plans.md` in this task -- B5 deletes the entire `docs/plans/` directory. Just copy the content and update the reference.

**Avoid:** Don't change the plan.json schema or any plan.json fields -- this task only changes where plan files are saved and where the schema reference lives.

## Steps

### Step 1: Copy schema reference to draft-plan skill directory

Copy the existing schema reference doc:

```bash
cp docs/plans/2026-03-19-structured-plans/design-structured-plans.md skills/draft-plan/schema-reference.md
```

The file is 303 lines and documents the full plan.json schema. It's the sole consumer of this file (draft-plan's `**See:**` reference), so it belongs alongside the skill.

### Step 2: Update plan directory path in SKILL.md

In `skills/draft-plan/SKILL.md`, replace all `docs/plans/` references with `.claude/claude-caliper/`.

**Line 10, current:**

```markdown
**Save to:** `docs/plans/YYYY-MM-DD-<topic>/` directory
```

**New:**

```markdown
**Save to:** `.claude/claude-caliper/YYYY-MM-DD-<topic>/` directory
```

**Line 23, current:**

```markdown
10. **Commit plan artifacts** — `git add docs/plans/<dir>/ && git commit -m "docs: add implementation plan for <topic>"`
```

**New (remove the git commit step -- plan artifacts are now gitignored):**

```markdown
10. **Skip** — plan artifacts are under `.claude/claude-caliper/` (gitignored), no commit needed
```

**Lines 31, current directory layout:**

```text
docs/plans/YYYY-MM-DD-topic/
```

**New:**

```text
.claude/claude-caliper/YYYY-MM-DD-topic/
```

### Step 3: Update schema reference See link

**Line 83, current:**

```markdown
**See:** `docs/plans/2026-03-19-structured-plans/design-structured-plans.md` for full schema reference.
```

**New:**

```markdown
**See:** `schema-reference.md` for full schema reference.
```

Since the reference doc is now in the same directory as the SKILL.md, use a relative path.

### Step 4: Verify changes

```bash
grep -c 'docs/plans/' skills/draft-plan/SKILL.md
```

Should output `0`.

```bash
test -f skills/draft-plan/schema-reference.md && echo "schema-reference.md exists"
grep 'schema-reference.md' skills/draft-plan/SKILL.md
```

Both should confirm the file exists and is referenced.
