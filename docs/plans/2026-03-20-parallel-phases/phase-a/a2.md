# A2: Update test fixture for new schema fields

> **Handoff from A1:** [TBD — Phase A dispatcher fills in after A1 completes]

**Avoid:** Don't change the fixture structure beyond adding the new fields — the render tests compare against `plan.md` which is generated from the fixture. If the fixture changes structurally, `test_render.sh` will break. Only add `workflow` and `depends_on` fields.

## Steps

### Step 1: Verify fixture state after A1

A1 already added `workflow` and `depends_on` to the fixture. Verify the fixture is valid:

```bash
bash scripts/validate-plan --schema tests/validate-plan/fixtures/valid-plan/plan.json
```

If A1 did this correctly, this step is a verification only.

### Step 2: Update expected plan.md if render output changed

Run render and compare:

```bash
bash scripts/validate-plan --render tests/validate-plan/fixtures/valid-plan/plan.json
diff tests/validate-plan/fixtures/valid-plan/plan.md <(bash scripts/validate-plan --render tests/validate-plan/fixtures/valid-plan/plan.json 2>/dev/null)
```

If `--render` now includes `workflow` in its output, update the expected `plan.md` to match. If render doesn't output workflow (it's metadata, not prose), no change needed.

### Step 3: Run full test suite

```bash
bash tests/validate-plan/test_schema.sh
bash tests/validate-plan/test_render.sh
bash tests/validate-plan/test_e2e.sh
```

All must pass. Commit.
