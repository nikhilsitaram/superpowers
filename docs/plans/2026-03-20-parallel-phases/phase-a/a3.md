# A3: Update draft-plan SKILL.md for workflow and phase depends_on

**Avoid:** Don't exceed 1000 words — draft-plan is already dense. Add the minimum necessary to document the new fields. Don't repeat information the agent already knows about JSON schema or dependency graphs.

## Steps

### Step 1: Add workflow field to plan.json example

In the plan.json example block in draft-plan SKILL.md, add `"workflow": "ship"` after the `"status"` field. This is the default value — the design skill sets the actual value after the user chooses.

### Step 2: Add depends_on to phase objects in the plan.json example

In the phases array example, add `"depends_on": []` to the example phase object. Show a brief comment or second phase example with `"depends_on": ["A"]` to illustrate cross-phase dependencies.

### Step 3: Document the fields in the Phasing section

In the Phasing section, add a brief note: "Each phase declares `depends_on` in plan.json — an array of phase letters this phase requires to complete first. Independent phases (empty or non-overlapping depends_on) can execute concurrently. The orchestrator uses this to build a dependency graph."

### Step 4: Add workflow to the plan.json fields documentation

Near the existing "Optional: `success_criteria` array..." line, add: "`workflow` field controls post-plan behavior: `ship` (full auto), `review-only` (execute but don't merge final PR), `plan-only` (stop after planning). Set by the design skill based on user choice."

### Step 5: Verify word count and commit

Count words: `wc -w skills/draft-plan/SKILL.md` — must be under 1000. If over, trim existing prose. Commit.
