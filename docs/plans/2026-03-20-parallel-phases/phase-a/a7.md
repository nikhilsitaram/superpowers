# A7: Update phase-dispatcher-prompt.md for integration branch PR targeting

**Avoid:** Don't change the dispatcher's core behavior — tasks still run sequentially within a phase. Only update the variables and context to reflect the integration branch model.

## Steps

### Step 1: Update REPO_PATH documentation

Change the `{REPO_PATH}` variable description from "working directory" to "phase worktree path (e.g., .claude/worktrees/<feature>-phase-a)". This clarifies that the dispatcher works in an isolated worktree, not the main repo.

### Step 2: Update PRIOR_COMPLETIONS scoping

Change the description from "concatenated completion.md content from prior phases" to "concatenated completion.md content from phases in the dependency chain (transitive closure of depends_on)." Update the inline comment from "Empty for Phase A" to "Empty for phases with no dependencies."

### Step 3: Verify no other changes needed

The dispatcher doesn't create PRs itself — that's handled by orchestrate after the dispatcher returns. Verify no references to `--base phase-{prior-letter}` or stacked PR logic exist in the prompt. The dispatcher only writes code, updates plan status, and writes completion notes.

### Step 4: Commit

Commit the changes.
