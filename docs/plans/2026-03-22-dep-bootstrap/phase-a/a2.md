# A2: Add bootstrap sub-step to design SKILL.md step 6

**Avoid:** Don't add a new top-level step. The design doc specifies splitting step 6 into sub-steps, not adding step 6.5 or renumbering. This keeps the checklist stable for agents that may have learned the step numbers.

**Avoid:** Don't use `@dependency-bootstrap.md` (force-loads the file). Use `**See:** ./dependency-bootstrap.md` so the agent reads it on-demand only when it reaches the bootstrap sub-step.

**Avoid:** Don't duplicate any content from dependency-bootstrap.md into SKILL.md. The whole point of the reference file is to keep the procedure in one place. The SKILL.md sub-step should be a one-liner pointing to the reference.

## Steps

### Step 1: Read current step 6

Read `skills/design/SKILL.md` and locate step 6. It currently reads:

```text
6. **Set up worktree** — `git worktree add -b integrate/<feature> .claude/worktrees/<feature>`; run tests to establish a clean baseline. This is the integration branch — phase worktrees are created by orchestrate as siblings.
```

### Step 2: Replace step 6 with sub-steps

Replace the current step 6 text with:

```text
6. **Set up worktree** — This is the integration branch — phase worktrees are created by orchestrate as siblings.
   1. Create worktree: `git worktree add -b integrate/<feature> .claude/worktrees/<feature>`
   2. Bootstrap dependencies per **See:** ./dependency-bootstrap.md
   3. Run tests to establish a clean baseline
```

The sub-step numbering (1, 2, 3) nests under step 6's indentation level, making it clear these are sub-steps not top-level steps.

### Step 3: Verify word count

```bash
wc -w skills/design/SKILL.md
```

Confirm total word count stays under 1,000. The change should be roughly neutral — the original step 6 was one long sentence, the replacement is three short sub-steps plus a brief header. Commit:

```text
feat: add dependency bootstrap sub-step to design skill worktree setup
```

## Handoff from A1

A1 is complete. `skills/design/dependency-bootstrap.md` now exists at commit `4832055fc00c9c8c555d7b502941acbf610cd3b2`. The file has 370 words and all 5 required sections. You can reference it as `./dependency-bootstrap.md` from within the design skill directory.
