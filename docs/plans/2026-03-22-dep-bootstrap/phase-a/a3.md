# A3: Replace orchestrate inline install table with See reference

**Avoid:** Don't remove the step 3 header or change its position in the execution flow. Only replace the body content (the install table and symlink fallback prose). The step number and its role in the per-phase execution sequence must stay the same.

**Avoid:** Don't use a relative path in the See reference. Orchestrate lives in `skills/orchestrate/` but the reference file is in `skills/design/`. Use the repo-relative path `skills/design/dependency-bootstrap.md` so the agent can find it regardless of working directory.

## Steps

### Step 1: Read current step 3 content

Read `skills/orchestrate/SKILL.md` and locate step 3 in the "Per-Phase Execution" section. It currently contains:

- A one-line intro: "**Bootstrap dependencies** in the phase worktree — detect lockfiles/manifests and run the matching install command."
- A table with 8 rows (pyproject.toml, requirements.txt, package-lock.json, yarn.lock, pnpm-lock.yaml, Cargo.toml, go.mod, None)
- A "Symlink fallback" paragraph explaining the fallback behavior
- A closing line: "Only runs once per phase — tasks inherit the environment."

This spans approximately lines 72-86 and totals ~180 words.

### Step 2: Replace with concise summary + See reference

Replace the entire step 3 (intro, table, symlink fallback paragraph, and closing line) with:

```text
3. **Bootstrap dependencies** in the phase worktree. **See:** skills/design/dependency-bootstrap.md
```

Keep it as a single line. The reference file contains the full detection order, install commands, Python fallback, and failure handling. No need to summarize any of it here.

### Step 3: Verify word reduction and total count

```bash
wc -w skills/orchestrate/SKILL.md
```

Confirm the edit reduced word count by approximately 150 words and total stays under 1,000. Commit:

```text
refactor: replace orchestrate inline dep install with shared reference
```
