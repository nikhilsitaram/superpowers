# A2: Rewrite design SKILL.md for execution mode selection

**Avoid:** Don't keep the "Prerequisite: Agent Teams" section — it's being replaced by mode-aware logic. Don't add the agent-teams env var check here; that check moves to `dispatch-agent-teams.md` (A3). Don't exceed the 2,000-word hard cap — the current file is already close to budget.

## Steps

### Step 1: Read the current design SKILL.md

Read `skills/design/SKILL.md` in full. Identify:
- The "Prerequisite: Agent Teams" section (lines ~10-18) — DELETE entirely
- Step 7 (workflow extent) and Step 8 (design approval gate) — MERGE into a single combined step
- The Key Constraints table reference to `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` — REMOVE

### Step 2: Remove the Agent Teams prerequisite section

Delete the entire "## Prerequisite: Agent Teams" section (the block that checks `$CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, offers to enable it, and stops). This is no longer a hard gate — agent-teams is one of three execution modes.

### Step 3: Add auto-suggestion logic

Add a new section before the combined decision point that explains the auto-suggestion thresholds. This goes right before what was step 7. The logic (evaluated in priority order, first match wins):

1. **>10 tasks OR >2 phases OR >3 tasks able to run in parallel** -> suggest `agent-teams`
2. **<=5 tasks** -> suggest `main`
3. **Everything else** -> suggest `subagents`

### Step 4: Replace steps 7+8 with combined AskUserQuestion

Replace the separate workflow extent (step 7) and design approval (step 8) with a SINGLE AskUserQuestion that captures all three decisions:

The combined question should offer execution mode choices with the auto-suggested option marked as default. Format:

```text
AskUserQuestion (header: "Workflow & Execution Mode"):

**Workflow extent:**
- Create PR (default) — Orchestrate -> pr-create
- Merge PR — Orchestrate -> pr-create -> pr-review -> pr-merge
- Plan only — Stop after plan

**Execution mode** (auto-suggested: {MODE}):
- Main context — sequential, lead implements directly
- Subagents — parallel Agent tool dispatch with worktree isolation
- Agent teams — parallel teammates with push notifications + mailbox

**Design approval:**
- Approve design (auto turn on acceptEdits)
- Needs changes
```

### Step 5: Add agent-teams fallback flow

After the combined decision point, add a conditional block: if the user selected `agent-teams` and `$CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` is not `1`, tell the user:

```text
Agent teams requires an env var that can't be set mid-session.
Run: export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
Add to your shell profile, then restart Claude Code and re-run your request.
```

Stop execution — do not proceed to draft-plan.

### Step 6: Update step 12 (route workflow) to pass execution_mode

After draft-plan returns, the design skill already writes `workflow` to plan.json. Add the `execution_mode` write alongside it:

```bash
jq --arg w "<workflow>" --arg m "<mode>" '.workflow = $w | .execution_mode = $m' plan.json > tmp && mv tmp plan.json
```

### Step 7: Word count check

```bash
wc -w skills/design/SKILL.md
```

Must be under 2,000 words. If over, trim verbose explanations — the auto-suggestion logic and agent-teams fallback should be concise.

### Step 8: Commit

```bash
git add skills/design/SKILL.md
git commit -m "feat: replace agent-teams prerequisite with execution mode selection in design skill"
```
