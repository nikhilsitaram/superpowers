# A6: Refactor orchestrate SKILL.md for execution_mode dispatch

**Avoid:** Don't duplicate dispatch logic inline — the whole point of this refactor is to move mode-specific dispatch into separate files (dispatch-main.md, dispatch-subagents.md, dispatch-agent-teams.md) and keep orchestrate SKILL.md as a thin coordinator. Don't remove the mode-agnostic sections (Setup, Prepare Phase, Phase Wrap-Up, Review Loop Protocol, After All Phases) — those are shared across all modes.

## Steps

### Step 1: Read the current orchestrate SKILL.md and all three dispatch files

Read:
- `skills/orchestrate/SKILL.md` (current state)
- `skills/orchestrate/dispatch-agent-teams.md` (A3)
- `skills/orchestrate/dispatch-subagents.md` (A4)
- `skills/orchestrate/dispatch-main.md` (A5)

Identify which sections of the current SKILL.md are mode-specific (should be removed) vs mode-agnostic (should stay).

**Mode-specific (remove):**
- "Spawn Implementer Teammates" section
- "Process Completions (Push-Based)" section
- "Handle Escalations" section
- Prompt Templates table (moved to dispatch files)
- Key Constraints row about `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`

**Mode-agnostic (keep):**
- Setup section (add execution_mode read)
- Prepare Phase section
- Phase Wrap-Up section
- Review Loop Protocol section
- Single-Phase Plans section
- After All Phases section
- Progress Tracking section

### Step 2: Add execution_mode read to Setup

In the Setup section, add after the workflow read:

```bash
EXECUTION_MODE=$(jq -r '.execution_mode' plan.json)
```

### Step 3: Replace inline dispatch with conditional See-reference

Replace the removed mode-specific sections with a single conditional reference block:

```text
### Task Dispatch

Read the dispatch protocol matching `execution_mode` from plan.json:
- **See:** `./dispatch-main.md` (when execution_mode is "main")
- **See:** `./dispatch-subagents.md` (when execution_mode is "subagents")
- **See:** `./dispatch-agent-teams.md` (when execution_mode is "agent-teams")

Read ONLY the file matching the plan's execution_mode. Follow that protocol for all task dispatch, completion handling, review feedback, and escalations within this phase.
```

### Step 4: Update the description frontmatter

The current description says "Use when executing implementation plans with independent tasks in the current session" — this is still accurate regardless of execution mode. Keep it.

Update the opening paragraph:

```text
Execute plans using the configured execution mode. Phases run sequentially; tasks within each phase execute according to the dispatch protocol (sequential for main, parallel for subagents/agent-teams).
```

### Step 5: Remove the agent-teams env var constraint

Delete the Key Constraints table row about `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`. This check now lives in `dispatch-agent-teams.md`.

### Step 6: Word count check

```bash
wc -w skills/orchestrate/SKILL.md
```

Should be significantly under 2,000 words (likely ~800-1000) since the bulk of dispatch logic moved to separate files.

### Step 7: Verify consistency

Check that the remaining mode-agnostic sections don't reference teammate-specific concepts (mailbox, teammate spawn, idle notifications) — those now live only in dispatch-agent-teams.md.

Search for terms that should not appear in the refactored SKILL.md:
- "teammate" (should be in dispatch-agent-teams.md only)
- "mailbox" (should be in dispatch-agent-teams.md only)
- "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS" (should be in dispatch-agent-teams.md only)

### Step 8: Commit

```bash
git add skills/orchestrate/SKILL.md
git commit -m "refactor: replace inline dispatch with execution_mode conditional references"
```
