# A3: Extract dispatch-agent-teams.md from current orchestrate SKILL.md

**Avoid:** Don't rewrite the dispatch protocol — extract it verbatim from the current orchestrate SKILL.md. The agent-teams dispatch is the current proven behavior; changing it during extraction risks breaking the existing workflow. The only addition is the env var check that moves from the design skill into this file.

## Steps

### Step 1: Identify extractable sections in orchestrate SKILL.md

Read `skills/orchestrate/SKILL.md`. The following sections contain agent-teams-specific dispatch logic:

- **Spawn Implementer Teammates** (teammate spawn, auto-provisioned worktree)
- **Process Completions (Push-Based)** (push notifications, mailbox feedback, incremental merge, dependency gate)
- **Handle Escalations** (mailbox-based Rule 4 violations)
- **Prompt Templates** table (references implementer-prompt.md and task-reviewer-prompt.md)

These become the content of `dispatch-agent-teams.md`.

### Step 2: Create dispatch-agent-teams.md

Create `skills/orchestrate/dispatch-agent-teams.md` with the extracted content. Structure:

```markdown
# Dispatch: Agent Teams

Prerequisite: verify `$CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` is `1`. If not set, stop with error — the design skill should have caught this.

## Prompt Templates

| Template | Purpose |
|----------|---------|
| `./implementer-prompt.md` | Task implementer teammate |
| `./task-reviewer-prompt.md` | Per-task reviewer teammate |

## Spawn Implementer Teammates

[Extract from current SKILL.md — teammate spawn for tasks with no unmet dependencies]

## Process Completions (Push-Based)

[Extract from current SKILL.md — the full 9-step completion handling:
1. Read completion notes
2. Dispatch reviewer teammate
3. Extract review-summary
4. Triage issues
5. Send fixes to original implementer via mailbox
6. Validate criteria
7. Kill teammate after review passes
8. Incremental merge
9. Dependency gate]

## Handle Escalations

[Extract from current SKILL.md — Rule 4 violations via mailbox]

## Review Feedback Loop

For agent-teams: send review feedback to the *original implementer* via mailbox messaging. The implementer still has full context and files. Implementer fixes and goes idle again. Repeat until review passes.
```

### Step 3: Verify content completeness

Compare the extracted file against the original orchestrate SKILL.md. Every agent-teams-specific behavior must be present:
- Teammate spawn with model, mode, description
- Push-based idle notifications (not polling)
- Mailbox feedback for review fixes
- Incremental merge after each task
- Dependency gate for blocked tasks
- Rule 4 escalations via mailbox

### Step 4: Commit

```bash
git add skills/orchestrate/dispatch-agent-teams.md
git commit -m "feat: extract agent-teams dispatch protocol to dispatch-agent-teams.md"
```
