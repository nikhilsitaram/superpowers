# A1: Add poll-review-merge sequence to orchestrate SKILL.md

**Avoid:** Don't add the full polling bash script inline in SKILL.md. The design doc shows the polling logic for architecture context, but SKILL.md is at 997 words — embedding multi-line bash would blow the 1000-word cap. Instead, describe the polling behavior in concise prose and let the agent write the bash at execution time.

**Avoid:** Don't re-poll after review-pr pushes fixes. The design doc explicitly calls this out as a non-goal to prevent infinite review loops.

**Avoid:** Don't change anything about the implementation-review step (step 6-9 in current numbering). The review gate is about external reviewers (CodeRabbit, Gemini) on the PR, not the internal implementation-review.

## Steps

### Step 1: Understand current structure

Read `skills/orchestrate/SKILL.md`. The current per-phase execution steps 14-16 are:

```text
14. Create phase PR: invoke create-pr with --base integrate/<feature>
15. Merge phase PR: gh pr merge --squash, then update integration worktree: git pull in .claude/worktrees/<feature>/
16. Clean up phase worktree: git worktree remove + git branch -D
```

The wave loop summary on line 61 reads:
```text
d. Process completions SERIALLY: review → triage → rebase → create-pr → merge → mark complete
```

The After All Phases section (step 5, line 113) reads:
```text
- "merge-pr": cd "$MAIN_REPO" first (merge-pr's worktree guard blocks execution from inside worktrees), then create final PR (integrate/<feature> → main), invoke review-pr then merge-pr, clean up integration worktree
```

### Step 2: Replace steps 14-16 with expanded steps 14-18

Replace the current steps 14-16 (lines 96-102 of `skills/orchestrate/SKILL.md`) with:

```text
14. Create phase PR: invoke create-pr with `--base integrate/<feature>`
15. Poll checks: `gh pr checks <NUMBER> --json bucket --jq '[.[] | select(.bucket == "pending")] | length'` every 60s. Max wait: `jq -r '.review_wait_minutes // 10' plan.json` minutes. On timeout: warn and proceed.
16. Review feedback: invoke review-pr with the phase PR number to read and address all reviewer comments
17. Merge phase PR: `gh pr merge --squash`, then update integration worktree: `git pull` in `.claude/worktrees/<feature>/`
18. Clean up phase worktree:
    ```bash
    git worktree remove .claude/worktrees/<feature>-phase-{letter}
    git branch -D phase-{letter}
    ```
```

Note: The old step 16 (clean up) becomes step 18 with the same content. Steps 15 (poll) and 16 (review-pr) are new insertions.

### Step 3: Update wave loop summary

Update the wave loop summary line (currently line 61) from:

```text
d. Process completions SERIALLY: review → triage → rebase → create-pr → merge → mark complete
```

to:

```text
d. Process completions SERIALLY: review → triage → rebase → create-pr → poll checks → review-pr → merge → mark complete
```

### Step 4: Update After All Phases section

Update the merge-pr workflow bullet (currently line 113) to include poll + review-pr for the final PR:

```text
- "merge-pr": `cd "$MAIN_REPO"` first, then create final PR (`integrate/<feature>` → main), poll checks (same mechanism as phase PRs), invoke review-pr, then merge-pr, clean up integration worktree
```

### Step 5: Update Continuity note

The continuity note on line 116 should remain as-is — it already says "Execute all phases, reviews, and PR creation continuously. Pause only for Rule 4 violations and the merge confirmation in merge-pr."

### Step 6: Verify word count

```bash
wc -w skills/orchestrate/SKILL.md
```

Must be <= 1000 words. The new steps add ~60 words (poll check description + review-pr invocation). To stay under the cap, tighten existing prose — candidates for compression:
- The reconciliation description in the wave loop (line 59) has verbose phrasing
- Step 3 "Bootstrap dependencies" can compress its See reference
- The Permission Model section has explanatory prose that could be tightened

If still over, the polling step description is the most compressible — reduce to: "Poll checks: `gh pr checks` every 60s, max `review_wait_minutes` from plan.json (default 10). Timeout → warn, proceed."

### Step 7: Commit

```text
feat(orchestrate): add poll-review gate after phase PR creation
```
