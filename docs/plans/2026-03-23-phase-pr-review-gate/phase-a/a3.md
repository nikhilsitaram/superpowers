# A3: Add review_wait_minutes to draft-plan SKILL.md schema docs

**Avoid:** Don't add verbose explanation of what `review_wait_minutes` does. The orchestrate skill documents the polling behavior — draft-plan only needs to document that the field exists and its default value. Adding behavioral descriptions here would duplicate content across skills and waste tokens.

**Avoid:** Don't change the plan.json example to include every optional field. The example should remain minimal. Add `review_wait_minutes` to the "Optional" paragraph below the example instead.

## Steps

### Step 1: Understand current draft-plan schema section

Read `skills/draft-plan/SKILL.md`. The plan.json example (lines 44-76) shows required fields. Below it (line 79) is:

```text
Optional: `success_criteria` array at plan, phase, and task levels for automated verification. `workflow` field controls post-plan behavior: `create-pr` (orchestrate + create-pr, default), `merge-pr` (orchestrate + create-pr + review-pr + merge-pr), `plan-only` (stop after planning). Set by the design skill based on user choice.
```

### Step 2: Add review_wait_minutes to the Optional paragraph

Update line 79 to append mention of `review_wait_minutes`:

```text
Optional: `success_criteria` array at plan, phase, and task levels for automated verification. `workflow` field controls post-plan behavior: `create-pr` (orchestrate + create-pr, default), `merge-pr` (orchestrate + create-pr + review-pr + merge-pr), `plan-only` (stop after planning). Set by the design skill based on user choice. `review_wait_minutes` integer sets the max wait for external reviewers (default 10, set 0 to skip polling).
```

This adds ~18 words.

### Step 3: Verify word count

```bash
wc -w skills/draft-plan/SKILL.md
```

Must be <= 1000 words. Current count is 999, so we need to offset the ~18 new words. Candidates for compression:

- Line 113 "Each phase declares `depends_on` in plan.json — an array of phase letters this phase requires to complete first. Independent phases (empty or non-overlapping depends_on) can execute concurrently. The orchestrator uses this to build a dependency graph." (41 words) can be compressed to "Each phase declares `depends_on` — phase letters required to complete first. Independent phases execute concurrently." (14 words, saving 27 words)
- Line 109 "Each phase gets its own directory (`phase-a/`, `phase-b/`) with a `completion.md` stub and task `.md` files. The phase's `rationale` field in plan.json explains why the phase exists." (30 words) can drop the second sentence since rationale is shown in the plan.json example (saving ~12 words)

Apply the minimum compression needed to stay at or under 1000 words.

### Step 4: Commit

```text
feat(draft-plan): document review_wait_minutes optional field in plan.json schema
```
