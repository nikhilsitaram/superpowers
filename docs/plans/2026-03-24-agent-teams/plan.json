{
  "schema": 1,
  "status": "Not Yet Started",
  "workflow": "create-pr",
  "goal": "Replace polling-based orchestration with agent teams for push-based parallel task execution",
  "architecture": "Rewrite orchestrate SKILL.md to use Claude Code agent teams (experimental). Lead spawns implementer teammates per task (parallel within phase), receives idle notifications (no polling), dispatches reviewer teammates as implementers finish. Phases execute sequentially. Delete phase-dispatcher-prompt.md — teammates self-manage lifecycle. Update validate-plan with file-set overlap detection and status consistency checks. Update draft-plan and plan-review to enforce file-set isolation.",
  "tech_stack": "Markdown (SKILL.md and prompt templates), Bash (validate-plan script), jq (JSON schema validation), Claude Code agent teams API (CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1)",
  "phases": [
    {
      "letter": "A",
      "name": "Agent teams migration",
      "status": "Not Started",
      "depends_on": [],
      "rationale": "Single phase per design doc. The orchestrator rewrite depends on the updated implementer/reviewer prompts, which depend on the validate-plan file-set checks. Splitting would create intermediate broken states where the skill references features that don't exist yet.",
      "tasks": [
        {
          "id": "A1",
          "name": "Remove supervision schema from validate-plan",
          "status": "pending",
          "depends_on": [],
          "files": {
            "create": [],
            "modify": [
              "scripts/validate-plan"
            ],
            "test": [
              "tests/validate-plan/test_supervision_schema.sh"
            ]
          },
          "verification": "bash tests/validate-plan/test_supervision_schema.sh && bash tests/validate-plan/test_schema.sh",
          "done_when": "supervision config object validation removed from do_schema(). The supervision schema test file updated: tests that checked acceptance of valid supervision configs now verify the field is irrelevant (or the test file is removed). Existing schema tests still pass."
        },
        {
          "id": "A2",
          "name": "Add file-set overlap detection to validate-plan",
          "status": "pending",
          "depends_on": [],
          "files": {
            "create": [
              "tests/validate-plan/test_fileset_overlap.sh"
            ],
            "modify": [
              "scripts/validate-plan"
            ],
            "test": [
              "tests/validate-plan/test_fileset_overlap.sh"
            ]
          },
          "verification": "bash tests/validate-plan/test_fileset_overlap.sh && bash tests/validate-plan/test_schema.sh",
          "done_when": "validate-plan --schema rejects plans where two tasks in the same phase share any file path across create, modify, or test arrays. Cross-phase overlap is allowed. Existing plans without overlap still pass. 6+ test cases pass: (1) no overlap passes, (2) same file in create of two tasks fails, (3) same file in modify of two tasks fails, (4) same file in test of two tasks fails, (5) create-modify cross-array overlap within phase fails, (6) cross-phase overlap passes."
        },
        {
          "id": "A3",
          "name": "Add status consistency checks to validate-plan",
          "status": "pending",
          "depends_on": [],
          "files": {
            "create": [
              "tests/validate-plan/test_status_consistency.sh"
            ],
            "modify": [
              "scripts/validate-plan"
            ],
            "test": [
              "tests/validate-plan/test_status_consistency.sh"
            ]
          },
          "verification": "bash tests/validate-plan/test_status_consistency.sh && bash tests/validate-plan/test_schema.sh",
          "done_when": "validate-plan --schema checks: (1) task ID prefix matches phase letter (A1 in Phase A, not B1), (2) phase letters are alphabetically ordered. Existing valid plans still pass. 4+ test cases pass covering valid plans, mismatched task ID prefixes, and out-of-order phase letters."
        },
        {
          "id": "A4",
          "name": "Update draft-plan skill for file-set isolation",
          "status": "pending",
          "depends_on": [],
          "files": {
            "create": [],
            "modify": [
              "skills/draft-plan/SKILL.md"
            ],
            "test": []
          },
          "verification": "wc -w skills/draft-plan/SKILL.md | awk '{if ($1 <= 2000) print \"PASS\"; else print \"FAIL: \" $1 \" words\"}'",
          "done_when": "draft-plan SKILL.md includes: (1) file-set isolation requirement — no two tasks in the same phase share any file path across create/modify/test, (2) updated phase execution model description — phases sequential, tasks parallel within phase, (3) removal of 'Independent phases execute concurrently' from Phasing section, (4) update completion.md stub note to reflect per-task completion files. Word count under 2,000."
        },
        {
          "id": "A5",
          "name": "Update plan-review skill for file-set isolation validation",
          "status": "pending",
          "depends_on": [],
          "files": {
            "create": [],
            "modify": [
              "skills/plan-review/SKILL.md",
              "skills/plan-review/reviewer-prompt.md"
            ],
            "test": []
          },
          "verification": "wc -w skills/plan-review/SKILL.md | awk '{if ($1 <= 2000) print \"PASS\"; else print \"FAIL: \" $1 \" words\"}' && wc -w skills/plan-review/reviewer-prompt.md | awk '{if ($1 <= 2000) print \"PASS\"; else print \"FAIL: \" $1 \" words\"}'",
          "done_when": "plan-review SKILL.md: (1) adds file-set isolation to 'What It Catches' structural validation section, (2) notes validate-plan --schema now checks file-set overlap. reviewer-prompt.md: (1) adds file-set isolation check to Phase Checks section — reviewer flags tasks in same phase that logically need to share files (indicating bad decomposition even if validate-plan didn't catch it). Word counts under 2,000 each."
        },
        {
          "id": "A6",
          "name": "Rewrite orchestrate SKILL.md for agent teams",
          "status": "pending",
          "depends_on": [],
          "files": {
            "create": [],
            "modify": [
              "skills/orchestrate/SKILL.md"
            ],
            "test": []
          },
          "verification": "wc -w skills/orchestrate/SKILL.md | awk '{if ($1 <= 2000) print \"PASS\"; else print \"FAIL: \" $1 \" words\"}' && grep -q 'CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS' skills/orchestrate/SKILL.md && grep -q 'teammate' skills/orchestrate/SKILL.md && ! grep -q 'sleep' skills/orchestrate/SKILL.md && echo PASS",
          "done_when": "Orchestrate SKILL.md fully rewritten. Includes: (1) environment gate — check CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 before execution, (2) sequential phase loop — no wave/DAG construction, (3) per-phase: spawn one implementer teammate per task (parallel), receive idle notifications (push-based, no sleep/poll), (4) dispatch reviewer teammate when implementer finishes, (5) after all implementers+reviewers done: merge task branches into feature branch, dispatch implementation review, run Review Loop Protocol, (6) single-phase shortcut, (7) multi-phase integration branch strategy, (8) cross-phase handoff notes written by lead between phases, (9) escalation via teammate mailbox messages to lead, (10) no references to phase-dispatcher-prompt.md. Word count under 2,000. No sleep commands."
        },
        {
          "id": "A7",
          "name": "Update implementer-prompt.md for teammate self-management",
          "status": "pending",
          "depends_on": [],
          "files": {
            "create": [],
            "modify": [
              "skills/orchestrate/implementer-prompt.md"
            ],
            "test": []
          },
          "verification": "grep -q 'validate-plan --update-status' skills/orchestrate/implementer-prompt.md && grep -q 'completion.md' skills/orchestrate/implementer-prompt.md && grep -q 'deviation' skills/orchestrate/implementer-prompt.md && echo PASS",
          "done_when": "implementer-prompt.md updated: (1) teammate marks own task in-progress via validate-plan --update-status at start, (2) teammate writes per-task completion notes to {PHASE_DIR}/{task_id_lower}-completion.md after implementation, (3) teammate marks own task complete via validate-plan --update-status, (4) deviation rules 1-3 (auto-fix bug, add critical, fix blocker) included in prompt, (5) deviation rule 4 (architectural change) — teammate sends message to lead via mailbox instead of stopping, (6) removed 'Work from: [directory]' placeholder — teammate uses auto-provisioned worktree, (7) prompt references the right variable names for agent teams context."
        },
        {
          "id": "A8",
          "name": "Update task-reviewer-prompt.md for teammate model",
          "status": "pending",
          "depends_on": [],
          "files": {
            "create": [],
            "modify": [
              "skills/orchestrate/task-reviewer-prompt.md"
            ],
            "test": []
          },
          "verification": "grep -q 'teammate' skills/orchestrate/task-reviewer-prompt.md && echo PASS",
          "done_when": "task-reviewer-prompt.md updated for teammate dispatch model: (1) reviewer is a teammate spawned by lead (not a subagent of phase dispatcher), (2) reviewer receives task completion file path ({PHASE_DIR}/{task_id_lower}-completion.md) to read implementer's notes, (3) git range context updated — reviewer gets the task branch diff (not a subrange within a shared worktree), (4) dispatch instructions updated (Agent tool → teammate spawn). 6-point checklist and output format unchanged."
        },
        {
          "id": "A9",
          "name": "Delete phase-dispatcher-prompt.md",
          "status": "pending",
          "depends_on": [
            "A6",
            "A7"
          ],
          "files": {
            "create": [],
            "modify": [],
            "test": []
          },
          "verification": "test ! -f skills/orchestrate/phase-dispatcher-prompt.md && echo PASS",
          "done_when": "phase-dispatcher-prompt.md deleted. No remaining references to it in any skill file (grep -r 'phase-dispatcher' skills/ returns nothing). Orchestrate SKILL.md prompt templates table updated to remove the row."
        },
        {
          "id": "A10",
          "name": "Bump version in marketplace.json",
          "status": "pending",
          "depends_on": [
            "A1",
            "A2",
            "A3",
            "A4",
            "A5",
            "A6",
            "A7",
            "A8",
            "A9"
          ],
          "files": {
            "create": [],
            "modify": [
              ".claude-plugin/marketplace.json"
            ],
            "test": []
          },
          "verification": "jq -r '.plugins[].version' .claude-plugin/marketplace.json | sort -u | grep -q '1.14.0'",
          "done_when": "All three plugin versions in marketplace.json bumped to 1.14.0 (from 1.13.0). Single consistent version across all three plugins."
        }
      ]
    }
  ]
}
