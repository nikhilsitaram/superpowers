# A2: Update draft-plan skill for file-set isolation

**Avoid:** Don't add a new "File-Set Isolation" section — integrate the constraint into existing sections to keep word count low. The skill is already at ~1,480 words; a new section risks hitting the 2,000 hard cap.

## Steps

### Step 1: Update the Phasing section

In `skills/draft-plan/SKILL.md`, find the Phasing section. Make these changes:

1. Remove `Independent phases (empty or non-overlapping) execute concurrently.` — phases are now sequential per the agent teams design.

2. Add after the `depends_on` sentence: `Tasks within a phase execute in parallel, so each task's file set (create + modify + test) must be disjoint from every other task in the same phase. validate-plan --schema rejects overlapping file sets.`

### Step 2: Update the completion.md stub reference

In the Plan Structure section, the directory layout shows `completion.md` per phase with the comment `# Empty stub (dispatcher fills)`. Update the comment to `# Empty stub (lead aggregates per-task completions)` since the phase dispatcher no longer exists and teammates write per-task `{task_id_lower}-completion.md` files.

### Step 3: Update the depends_on field description

In the Task Structure table, the `depends_on` field's "Good" example says `["A1", "A2"] (same/prior phase only)`. Since tasks within a phase now run in parallel, intra-phase `depends_on` is used for semantic ordering in the agent teams task dependency system. Keep the field but update: `["A1", "A2"] (same phase for semantic ordering, prior phase for cross-phase deps)`

### Step 4: Update the Handoff notes lines

Two dispatcher references to update:
1. Around line 97: `the source phase's dispatcher appends a handoff section after the H1` → change "the source phase's dispatcher" to "the lead"
2. Around line 141: `The dispatcher appends handoff sections to cross-phase task files. Draft-plan doesn't write these.` → change to `The lead writes handoff sections to cross-phase task files between phases. Draft-plan doesn't write these.`

### Step 5: Verify word count

```bash
wc -w skills/draft-plan/SKILL.md
```

Must be under 2,000 words.

### Step 6: Commit

```bash
git add skills/draft-plan/SKILL.md
git commit -m "docs: update draft-plan skill for file-set isolation and sequential phases"
```
