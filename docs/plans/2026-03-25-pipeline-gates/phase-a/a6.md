# A6: Update draft-plan skill doc with entry gate

**Avoid:** Don't restructure the workflow list. The existing numbered steps (1-10) are well established. Insert the new gate check as a new step between the current step 1 (Initialize) and step 2 (Explore codebase), then renumber subsequent steps.

## Steps

### Step 1: Add entry gate step to the Workflow section of `skills/draft-plan/SKILL.md`

The current workflow in the SKILL.md file lists:

```text
1. **Initialize** — `TaskList` to check for prior session context
2. **Explore codebase** — Understand patterns, find exact file paths
...
```

Insert a new step 2:

```markdown
2. **Entry gate** — `scripts/validate-plan --check-entry $PLAN_DIR/plan.json --stage draft-plan` (exits early if design-review hasn't passed; the plan.json file need not exist yet — only reviews.json is read)
```

Renumber the remaining steps (old 2 becomes 3, old 3 becomes 4, etc., through old 10 becoming 11).

### Step 2: Verify

```bash
grep -q 'check-entry' skills/draft-plan/SKILL.md
```

The `$PLAN_DIR` in the command refers to the plan directory the agent is working in. The draft-plan skill knows this path because it's passed the design doc path from which it derives the plan directory.

Commit: `docs: add entry gate to draft-plan skill doc`
