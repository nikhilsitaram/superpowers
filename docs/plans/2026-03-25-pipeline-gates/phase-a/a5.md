# A5: Update orchestrate skill docs with gate calls

**Avoid:** Don't add more than a few lines per section. SKILL.md files have a 2,000-word hard cap, and the orchestrate skill is already ~1,100 words. Keep additions surgical — a bullet point or one-liner per gate, not a paragraph explaining what gates do.

## Steps

### Step 1: Add gate calls to the Setup section of `skills/orchestrate/SKILL.md`

In the Setup section (the "Before first phase" block), add three gate validation calls after the existing `--schema` validation line (`Validate schema: scripts/validate-plan --schema plan.json`):

```markdown
- Validate entry gate: `scripts/validate-plan --check-entry plan.json --stage execution`
- Validate base branch: `scripts/validate-plan --check-base plan.json`
- Validate consistency: `scripts/validate-plan --consistency plan.json`
```

Place these immediately after the existing `Validate schema` bullet.

### Step 2: Add per-phase gate calls to the Per-Phase Execution section

In the "Prepare Phase" subsection, add a bullet after creating the phase worktree:

```markdown
2. Re-validate base branch: `scripts/validate-plan --check-base plan.json` (multi-phase only — ensures dispatch happens from integration worktree, not main)
```

In the "Phase Wrap-Up" subsection, after the `--update-status` call (step 5), add:

```markdown
6. Re-validate consistency: `scripts/validate-plan --consistency plan.json` (catches state drift after status updates)
```

Renumber subsequent steps.

### Step 3: Add consistency check after plan status updates

In the "After All Phases (Multi-Phase Only)" section, after step 4 (`--update-status plan.json --plan --status Complete`), add:

```markdown
5. Re-validate consistency: `scripts/validate-plan --consistency plan.json`
```

In the "Single-Phase Plans" section, after step 5 (`--update-status plan.json --plan --status Complete`), add:

```markdown
6. Re-validate consistency: `scripts/validate-plan --consistency plan.json`
```

Renumber subsequent steps in both sections.

### Step 4: Update the Key Constraints table

Add a row:

```markdown
| Run gate checks at startup and after status changes | Entry gates prevent wasted work, base-branch checks prevent wrong-worktree dispatch, consistency checks catch state drift |
```

### Step 5: Add notes to dispatch docs

In `skills/orchestrate/dispatch-subagents.md`, add a note at the end of the "Dispatch Implementers" section:

```markdown
**Note:** `--check-base` runs at orchestrate startup and before each phase dispatch (multi-phase). No separate dispatch-level base check is needed.
```

Add the same note to the corresponding section in `skills/orchestrate/dispatch-agent-teams.md` (the "Spawn Implementer Teammates" section).

### Step 6: Verify

```bash
grep -q 'check-entry' skills/orchestrate/SKILL.md
grep -q 'check-base' skills/orchestrate/SKILL.md
grep -q 'consistency' skills/orchestrate/SKILL.md
grep -q 'check-base' skills/orchestrate/dispatch-subagents.md
grep -q 'check-base' skills/orchestrate/dispatch-agent-teams.md
```

Commit: `docs: add gate calls to orchestrate skill docs`
