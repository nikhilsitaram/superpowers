# A1: Remove nested regression test invocations

**Avoid:** Do not delete any actual test logic (Tests 1-11 in test_check_review.sh, Tests 1-10 in test_review_gates.sh). Only remove the "still passes" regression re-runs that invoke other test files. Do not change the test count variable logic or final summary — adjust the expected count in any assertions if present.

## Steps

### Step 1: Remove Test 12 from test_check_review.sh

Delete lines 125-127 in `tests/validate-plan/test_check_review.sh`:

```bash
echo "Test 12: Run existing test suites still pass"
assert_pass "test_schema.sh still passes" bash "$SCRIPT_DIR/test_schema.sh"
assert_pass "test_update_status.sh still passes" bash "$SCRIPT_DIR/test_update_status.sh"
```

The file should end after Test 11's assertion, then the blank line, results summary, and exit check (lines 129-131).

### Step 2: Remove Test 11 from test_review_gates.sh

Delete lines 153-154 in `tests/validate-plan/test_review_gates.sh`:

```bash
echo "Test 11: Run existing update-status tests still pass"
assert_pass "test_update_status.sh still passes" bash "$SCRIPT_DIR/test_update_status.sh"
```

The file should end after Test 10's assertion, then the blank line, results summary, and exit check (lines 156-158).

### Step 3: Run both test files and verify

```bash
bash tests/validate-plan/test_check_review.sh
bash tests/validate-plan/test_review_gates.sh
```

Expected: test_check_review.sh reports "11 passed, 0 failed", test_review_gates.sh reports "11 passed, 0 failed" (Tests 1-10 + 5b). Each completes in under 2 seconds.

### Step 4: Commit

```bash
git add tests/validate-plan/test_check_review.sh tests/validate-plan/test_review_gates.sh
git commit -m "perf: remove nested regression test invocations from check-review and review-gates tests"
```
