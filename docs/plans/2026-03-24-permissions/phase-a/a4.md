# A4: Verify no dead permission-forwarding code

**Avoid:** Don't modify any files. This is a read-only verification task.

## Steps

### Step 1: Search for dead supervision/polling code in hooks

Search the hooks directory for any remnant of the old supervision loop that forwarded permission prompts:

```bash
grep -rn 'forward\|poll\|supervision\|stuck\|dispatch.*permission' hooks/
```

Expected: No matches related to permission forwarding. The hooks directory should only contain the current PreToolUse and PermissionRequest handlers.

### Step 2: Search orchestrate skill for permission forwarding

```bash
grep -rn 'permission\|forward.*prompt\|poll.*agent' skills/orchestrate/
```

Expected: No matches related to permission-prompt forwarding from the old supervision loop. The old supervision loop was a design doc + plan that was replaced entirely by agent teams in PR #123. No runtime code was ever shipped.

### Step 3: Confirm dead code status

If both searches return clean (no dead permission-forwarding code), the task is done. If any dead code is found, document the file paths and line numbers -- but do NOT remove code in this task. File a follow-up finding instead.

The preliminary analysis from the design doc says: "the old supervision loop was a design doc + plan, replaced entirely by agent teams (PR #123). No runtime code to remove." This step confirms that analysis.
