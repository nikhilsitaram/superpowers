# A1: Add missing commands to safe-commands.txt

**Avoid:** Don't reorder existing entries or change spacing. The file is line-delimited with one command per line. Append new commands in the appropriate section (Unix tools section, before the shell keywords section).

## Steps

### Step 1: Add 5 commands to hooks/safe-commands.txt

Add these commands after the existing Unix tools block (after `xargs`) and before the shell keywords block (before `case`):

```text
ln
dirname
basename
[
command
```

The file has two logical sections: Unix tools (lines 1-42) and shell keywords (lines 43-59). Insert the new commands at the end of the Unix tools section, before the shell keywords.

### Step 2: Verify existing tests still pass

Run:

```bash
bash tests/hooks/test_safe_commands.sh
```

All existing tests must pass. The new commands don't conflict with any existing test expectations because no test uses these commands as "unsafe" examples.

### Step 3: Verify the bundled safe list includes the new commands

```bash
grep -c '[a-zA-Z\[]' hooks/safe-commands.txt
```

Should show 64 (was 59, added 5).

### Step 4: Bump version in marketplace.json

Bump `version` from `1.14.0` to `1.15.0` across all three plugin entries in `.claude-plugin/marketplace.json`. This is required per project CLAUDE.md for any PR that changes hook behavior — the plugin installer compares cached vs declared version.
