# A3: Update marketplace.json, CLAUDE.md, and README.md

**Avoid:** Bump to `1.15.0` as specified (minor bump for the new rebase and merge-continuation features). Bump all three `"version"` fields in marketplace.json identically (they must stay in sync). In README.md, the mermaid diagram node IDs (S, MR, MG) are internal identifiers — only update the display labels shown inside brackets `[]`, not the node IDs themselves.

## Steps

### Step 1: Update .claude-plugin/marketplace.json

**Bump version** from `1.14.0` to `1.15.0` in all three plugin entries (lines 10, 39, 61).

**Update skill paths** in the `claude-caliper` bundle (lines 28-30):
```json
"./skills/pr-create",
"./skills/pr-review",
"./skills/pr-merge",
```

**Update skill paths** in the `claude-caliper-workflow` bundle (lines 49-51):
```json
"./skills/pr-create",
"./skills/pr-review",
"./skills/pr-merge",
```

**Update the workflow bundle description** (line 34):
```json
"description": "End-to-end development workflow: design → draft-plan → orchestrate → review → pr-create → pr-review → pr-merge",
```

The `claude-caliper-tooling` bundle has no PR skills — no changes needed there.

Verify:
```bash
! grep -E 'create-pr|review-pr|merge-pr' .claude-plugin/marketplace.json && echo "OK"
```

### Step 2: Update CLAUDE.md

Update the Development Workflow section (around line 30):
- `design -> worktree -> draft-plan -> orchestrate -> create-pr -> review-pr -> merge-pr` -> `design -> worktree -> draft-plan -> orchestrate -> pr-create -> pr-review -> pr-merge`

Verify:
```bash
! grep -E '\bcreate-pr\b|\breview-pr\b|\bmerge-pr\b' CLAUDE.md && echo "OK"
```

### Step 3: Update README.md

Multiple sections need updates:

1. **Step table** (around lines 49-54):
   - Row 9: `Create PR` skill reference -> keep label, update path `create-pr` -> `pr-create`
   - Row 10: `You review the PR and run /review-pr` -> `You review the PR and run /pr-review`
   - Row 13: `You run /merge-pr` -> `You run /pr-merge`

2. **Mermaid diagram** (around lines 80-84):
   - Node S label: `S[create-pr]` -> `S[pr-create]`
   - Node MR label: `MR[review-pr]` -> `MR[pr-review]`
   - Node MG label: `MG[merge-pr]` -> `MG[pr-merge]`

3. **Skills Reference table** (around lines 159-161):
   - `| **Create PR** | [create-pr](skills/create-pr/) |` -> `| **Create PR** | [pr-create](skills/pr-create/) |`
   - `| **Review PR** | [review-pr](skills/review-pr/) |` -> `| **Review PR** | [pr-review](skills/pr-review/) |`
   - `| **Merge** | [merge-pr](skills/merge-pr/) |` -> `| **Merge** | [pr-merge](skills/pr-merge/) |`

4. **FAQ section** (around line 475):
   - `After approving the design, you choose: **Create PR** (execute and open PR for human review), **Merge PR** (execute, open PR, review, and merge)` — this describes user-facing choices, not skill names. Keep the user-facing labels but verify no skill path references remain.

5. **plan.json example** (around line 299): `"workflow": "create-pr"` -> `"workflow": "pr-create"`

6. **Prose references throughout** — search for any remaining `create-pr`, `review-pr`, `merge-pr` strings and update them. Key locations:
   - Line 28: "The plugin installs 11 skills..." — check for old names in the list
   - "review-pr reviewer" in the fresh context section (around line 183) -> "pr-review reviewer"

Verify:
```bash
! grep -E '\bcreate-pr\b|\breview-pr\b|\bmerge-pr\b' README.md && echo "OK"
```

### Step 4: Commit

```bash
git add .claude-plugin/marketplace.json CLAUDE.md README.md
git commit -m "refactor: update marketplace paths, docs, and README to pr-* names"
```
