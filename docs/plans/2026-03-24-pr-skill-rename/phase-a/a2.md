# A2: Update cross-references in non-PR skills

**Avoid:** Do not change any logic or workflow behavior in these files — only update name references. The workflow enum values in design/SKILL.md and draft-plan/SKILL.md must change from `create-pr`/`merge-pr` to `pr-create`/`pr-merge` because they reference the plan.json enum (which is being renamed in A4). Do not touch the PR skill SKILL.md files (those are A1's file set).

## Steps

### Step 1: Update skills/design/SKILL.md

This file has the most references. Update the following:

1. **Step 7 workflow options** (around line 46-48): Update the AskUserQuestion options and their descriptions:
   - `create-pr` -> `pr-create` in the comment about default
   - `Orchestrate → create-pr` -> `Orchestrate → pr-create`
   - `Orchestrate → create-pr → review-pr → merge-pr` -> `Orchestrate → pr-create → pr-review → pr-merge`

2. **Step 12 enum mapping** (around line 56): Update:
   - `Create PR` → `create-pr` -> `Create PR` → `pr-create`
   - `Merge PR` → `merge-pr` -> `Merge PR` → `pr-merge`
   - References to `create-pr` and `merge-pr` workflow values -> `pr-create` and `pr-merge`

Verify:
```bash
! grep -E '\bcreate-pr\b|\breview-pr\b|\bmerge-pr\b' skills/design/SKILL.md && echo "OK"
```

### Step 2: Update skills/orchestrate/SKILL.md

Update all cross-references:

1. **Single-Phase Plans section** (around lines 107-108):
   - `"create-pr"`: invoke create-pr -> `"pr-create"`: invoke pr-create
   - `"merge-pr"`: invoke create-pr, poll checks + review-pr --automated -> `"pr-merge"`: invoke pr-create, poll checks + pr-review --automated
   - `then merge-pr with --squash` -> `then pr-merge with --squash`

2. **After All Phases section** (around lines 116-118):
   - `"merge-pr"`: create final PR, ... review-pr --automated, merge-pr with --rebase` -> `"pr-merge"`: create final PR, ... pr-review --automated, pr-merge with --rebase`
   - `"create-pr"`: create final PR -> `"pr-create"`: create final PR

3. **Continuity note** (line 120):
   - `merge confirmation in merge-pr` -> `merge confirmation in pr-merge`

4. **Integration section** (line 135):
   - `design → draft-plan → **this skill** → create-pr → review-pr → merge-pr` -> `design → draft-plan → **this skill** → pr-create → pr-review → pr-merge`

Verify:
```bash
! grep -E '\bcreate-pr\b|\breview-pr\b|\bmerge-pr\b' skills/orchestrate/SKILL.md && echo "OK"
```

### Step 3: Update skills/draft-plan/SKILL.md

Update the workflow enum documentation:

1. **plan.json example** (around line 48): The example shows `"workflow": "create-pr"` — update to `"workflow": "pr-create"`

2. **Optional field documentation** (around line 79): Update:
   - `create-pr` (orchestrate + create-pr, default) -> `pr-create` (orchestrate + pr-create, default)
   - `merge-pr` (orchestrate + create-pr + review-pr + merge-pr) -> `pr-merge` (orchestrate + pr-create + pr-review + pr-merge)

Verify:
```bash
! grep -E '\bcreate-pr\b|\breview-pr\b|\bmerge-pr\b' skills/draft-plan/SKILL.md && echo "OK"
```

### Step 4: Update skills/implementation-review/SKILL.md

Update the Integration section (around line 79):
- `**Leads to:** create-pr (once review passes)` -> `**Leads to:** pr-create (once review passes)`

Verify:
```bash
! grep -E '\bcreate-pr\b|\breview-pr\b|\bmerge-pr\b' skills/implementation-review/SKILL.md && echo "OK"
```

### Step 5: Final verification across all four files

```bash
! grep -rE '\bcreate-pr\b|\breview-pr\b|\bmerge-pr\b' skills/design/SKILL.md skills/orchestrate/SKILL.md skills/draft-plan/SKILL.md skills/implementation-review/SKILL.md && echo "All references updated"
```

Commit:
```bash
git add skills/design/SKILL.md skills/orchestrate/SKILL.md skills/draft-plan/SKILL.md skills/implementation-review/SKILL.md
git commit -m "refactor: update cross-references in non-PR skills to pr-* names"
```
