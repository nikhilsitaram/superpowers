# A4: Switch all 5 subagent prompt files from bypassPermissions to auto

**Avoid:** Don't change anything except the `mode:` line in each file. The prompt content, structure, and model selection stay identical. This is a targeted one-line change per file.

## Steps

### Step 1: Update implementer-prompt.md

In `skills/orchestrate/implementer-prompt.md`, change:

```text
  mode: "bypassPermissions"
```

to:

```text
  mode: "auto"
```

### Step 2: Update phase-dispatcher-prompt.md

In `skills/orchestrate/phase-dispatcher-prompt.md`, change:

```text
  mode: "bypassPermissions"
```

to:

```text
  mode: "auto"
```

### Step 3: Update task-reviewer-prompt.md

In `skills/orchestrate/task-reviewer-prompt.md`, change:

```text
  mode: "bypassPermissions"
```

to:

```text
  mode: "auto"
```

### Step 4: Update implementation-review reviewer-prompt.md

In `skills/implementation-review/reviewer-prompt.md`, change:

```text
  mode: "bypassPermissions"
```

to:

```text
  mode: "auto"
```

### Step 5: Update merge-pr reviewer-prompt.md

In `skills/merge-pr/reviewer-prompt.md`, change:

```text
  mode: "bypassPermissions"
```

to:

```text
  mode: "auto"
```

### Step 6: Verify no bypassPermissions remains

```bash
grep -r 'bypassPermissions' skills/
```

Should return zero results. If any remain, fix them. Commit:

```text
feat: switch all subagent prompts from bypassPermissions to auto mode
```
